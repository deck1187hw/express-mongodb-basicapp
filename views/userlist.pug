extends layout

block content
    h1.
        User List
    ul
        each user, i in userlist
            li
                a #[a(href="usersingle/#{user._id}") #{user.username} #[span]]

    p: This is a #[a(href="newuser") Add user #[span]]


